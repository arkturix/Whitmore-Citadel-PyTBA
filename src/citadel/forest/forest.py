from citadel.common.area import Area


class Forest1(Area):

    def __init__(self, name: str = "Forest 1", terrain: str = "Forest", description: str = None):
        super().__init__(name, terrain=terrain, description=description)


class Forest2(Area):

    def __init__(self, name: str = "Forest 2", terrain: str = "Forest", description: str = None):
        super().__init__(name, terrain=terrain, description=description)


class Forest3(Area):

    def __init__(self, name: str = "Forest 3", terrain: str = "Forest", description: str = None):
        super().__init__(name, terrain=terrain, description=description)


class Forest4(Area):

    def __init__(self, name: str = "Forest 4", terrain: str  = "Forest", description: str = None):
        super().__init__(name, terrain=terrain, description=description)


class Forest5(Area):

    def __init__(self, name: str = "Forest 5", terrain: str  = "Forest", description: str = None):
        super().__init__(name, terrain=terrain, description=description)


class Forest6(Area):

    def __init__(self, name: str = "Forest 6", terrain: str  = "Forest", description: str = None):
        super().__init__(name, terrain=terrain, description=description)


class Forest7(Area):

    def __init__(self, name: str = "Forest 7", terrain: str  = "Forest", description: str = None):
        super().__init__(name, terrain=terrain, description=description)
